# ══════════════════════════════════════════════════════════════
#  IntrusionX SE — Backend Dependencies (Unpinned for Compatibility)
#  Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── API Framework ─────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Data Validation ───────────────────────────
pydantic
pydantic-settings

# ── PostgreSQL (async) ────────────────────────
asyncpg

# ── Firebase Admin SDK ────────────────────────
firebase-admin

# ── HTTP Client ───────────────────────────────
httpx[socks]    # [socks] extra enables SOCKS5 proxy support for Tor routing

# ── DNS Resolution ────────────────────────────
dnspython

# ── EXIF / Image Forensics ────────────────────
exifread

# ── HTML Scraping ─────────────────────────────
beautifulsoup4
lxml          # fast HTML parser backend for bs4

# ── Browser Automation (Playwright skeleton) ──
playwright   # run: playwright install chromium

# ── CLI Rendering / Progress UI ───────────────
rich

# ── Environment ───────────────────────────────
python-dotenv
